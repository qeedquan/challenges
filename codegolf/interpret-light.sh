#!/bin/sh

cat << EOF >/dev/null

Interpreting ℒight is easy since all you do is print # indefinitely with no input.

The difference i have is that this prints a different symbol. Which is kind of useless imo

EOF

yes '#'
