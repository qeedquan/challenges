/*

With the given text below, there are some words in the text that repeats several times in the text.
Use any programming language to write a short code that compresses the text to display it.
Or in other words, use the smallest number of bytes to display the text.

The Text is:

Peter Piper picked a peck of pickled peppers.
A peck of pickled peppers Peter Piper picked.
If Peter Piper picked a peck of pickled peppers,
Where's the peck of pickled peppers Peter Piper picked?

*/

#include <stdio.h>

int
main(void)
{
	printf("Peter Piper picked a peck of pickled peppers.\n"
	       "A peck of pickled peppers Peter Piper picked.\n"
	       "If Peter Piper picked a peck of pickled peppers,\n"
	       "Where's the peck of pickled peppers Peter Piper picked?\n");

	return 0;
}
