/*

Simple: find two values a and b such that a == b, !a is true, and !b is false.

No strings allowed.

*/

var assert = require('assert');

const a = 0;
const b = [0];

assert(a == b);
assert(!a == true);
assert(!b == false);
