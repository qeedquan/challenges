# Running

```
# Need rpcbind to be running as a daemon before server/client can register to RPC services
service rpcbind start
```

```
# Run the server and client
./server localhost
./client localhost
```
