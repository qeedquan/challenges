#!/usr/bin/env python3

"""

mu = mean
sigma = variance

Var(X) = E(X^2) - mu^2 = sigma^2

If the mean and variance of a random variable X is
mu[x] and sigma[x]^2

Then the mean and standard deviation of a random variable Y = aX + b is
mu[y]      = a*mu[x] + b
sigma[y]^2 = a^2 * sigma[x]^2
sigma[y]   = |a| * sigma[x]

"""
